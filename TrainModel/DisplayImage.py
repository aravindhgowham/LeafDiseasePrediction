from Library import *

def PlotImage(img_arr, label):
    #
    # slic_image = img_arr[:]
    # slic_label = label[:]
    #
    # if slic_image and slic_label:
    #     for Image, Label in zip(slic_image, slic_label):
    #         plt.figure(figsize=(5,5))
    #         plt.imshow(Image) #Image/255
    #         plt.show()
    pass

